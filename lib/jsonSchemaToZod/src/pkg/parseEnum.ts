// import { Schema } from "../types";
// import { z } from "zod";
//
// export const parseEnum = (schema: Schema): z.ZodAny => {
//     if (!schema.enum || schema.enum.length === 0) {
//         return z.se
//     } else{
//         // union does not work when there is only one element
//         return z.enum(schema.enum);
//     }
// // };
